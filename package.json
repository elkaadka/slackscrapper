{
  "name": "slack_parser",
  "version": "1.0.0",
  "description": "A small nodejs project to parse a slack channel and looks for URLs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "slack"
  ],
  "author": "Adil El Kanabi",
  "license": "ISC",
  "dependencies": {
    "@slack/client": "^3.14.1",
    "colors": "^1.1.2"
  }
}
